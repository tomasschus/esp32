#pragma once

#include <lvgl.h>

/** Crea los widgets del menú principal. */
void screen_main_menu_create();

/** Devuelve el objeto de pantalla del menú principal. */
lv_obj_t *screen_main_menu_get();
